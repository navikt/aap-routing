management:
    endpoints:
        web:
            exposure:
                include: '*'
    endpoint:
        health:
            show-details: always
        shutdown:
            enabled: true
saf:
   scope: api://dev-fss.teamdokumenthandtering.saf${suffix}/.default
dokarkiv:
    scope: api://dev-fss.teamdokumenthandtering.dokarkiv${suffix}/.default
fordeling:
    enabled: true
    topics:
        main: teamdokumenthandtering.aapen-dok-journalfoering${suffix}
        backoff: 2000
oppgave:
    base-uri: https://oppgave${suffix}.dev-fss-pub.nais.io
    scope: api://dev-fss.oppgavehandtering.oppgave${suffix}/.default