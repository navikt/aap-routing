query($ident: ID!) {
    hentGeografiskTilknytning(ident: $ident) {
       gtType
       gtKommune
       gtBydel
       gtLand
    }
  }
query($ident: ID!) {
    hentPerson(ident: $ident)  {
    adressebeskyttelse {
       gradering
    }
  }
}