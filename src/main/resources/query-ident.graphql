query($ident: ID!, $grupper: [IdentGruppe!] = [AKTOERID], $historikk:Boolean = false) {
        hentIdenter(ident: $ident, grupper:$grupper, historikk:$historikk) {
            identer {
                   ident
                   gruppe
            }
       }
}